CREATE TABLE USUARIOPESSOA (ID BIGINT NOT NULL, EMAIL VARCHAR(255), IDADE INTEGER, LOGIN VARCHAR(255), NOME VARCHAR(255), SENHA VARCHAR(255), SOBRENOME VARCHAR(255), PRIMARY KEY (ID))
CREATE TABLE TELEFONEUSER (ID BIGINT NOT NULL, NUMERO VARCHAR(255) NOT NULL, TIPO VARCHAR(255) NOT NULL, USUARIOPESSOA_ID BIGINT, PRIMARY KEY (ID))
ALTER TABLE TELEFONEUSER ADD CONSTRAINT FK_TELEFONEUSER_USUARIOPESSOA_ID FOREIGN KEY (USUARIOPESSOA_ID) REFERENCES USUARIOPESSOA (ID)
CREATE TABLE SEQUENCE (SEQ_NAME VARCHAR(50) NOT NULL, SEQ_COUNT DECIMAL(38), PRIMARY KEY (SEQ_NAME))
INSERT INTO SEQUENCE(SEQ_NAME, SEQ_COUNT) values ('SEQ_GEN', 0)
